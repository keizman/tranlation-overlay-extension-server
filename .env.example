# Translation Overlay Extension Server Configuration
# Copy this file to .env and modify as needed

# =====================
# Authentication
# =====================
# Client -> Server authentication token (extension uses this to authenticate with server)
SITE_AUTH_TOKEN=YXBpLTEyMzQ1Ng==

# Server -> LLM authentication token (if set, overrides site_auth from request header)
# Priority: LLM_SITE_AUTH (env) > site_auth (header) > client_token
LLM_SITE_AUTH=

# =====================
# LLM Endpoint
# =====================
# Default LLM API endpoint (used if site_api header is not provided)
DEFAULT_LLM_ENDPOINT=http://127.0.0.1:8317/v1/chat/completions

# =====================
# Redis Cache
# =====================
# Redis connection string
REDIS_CONN_STRING=redis://localhost:6379/0

# Default cache TTL in days (can be overridden via Redis config)
DEFAULT_CACHE_TTL_DAYS=30

# =====================
# Logging
# =====================
# Directory for request/response logs
LOG_DIR=logs

# Maximum log file size in MB before rotation
MAX_LOG_SIZE_MB=300
